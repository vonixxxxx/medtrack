# FastAPI - Modern, fast web framework for building APIs with Python
fastapi==0.104.1

# Uvicorn - ASGI server for running FastAPI applications
uvicorn[standard]==0.24.0

# HTTPX - Async HTTP client for making requests to external APIs
httpx==0.25.2

# BeautifulSoup4 - HTML parsing for web scraping
beautifulsoup4==4.12.2

# LXML - XML/HTML parser for BeautifulSoup
lxml==4.9.3

# Pydantic - Data validation using Python type annotations (included with FastAPI)
# No need to install separately as it's a dependency of FastAPI

# Python-multipart - For handling form data (included with FastAPI)
# No need to install separately as it's a dependency of FastAPI
